import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // If Next.js blocks your local IP during development, 
  // you may need to run `npm run dev -- -H 0.0.0.0` instead.
};

export default nextConfig;